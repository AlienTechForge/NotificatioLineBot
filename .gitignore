# ── Java / Maven ──────────────────────────────────
target/
*.class
*.jar
# Maven wrapper 若日後加入，wrapper jar 需進版控
!.mvn/wrapper/maven-wrapper.jar

# ── 機密：絕對不可進版控 ────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Node / admin-ui ───────────────────────────────
node_modules/
dist/
.vite/
npm-debug.log*

# ── IDE ───────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath

# ── OS ────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 其他 ──────────────────────────────────────────
*.log

# ── 部署產物（含機密，絕不進版控）────────────────
deploy/.deploy_key
deploy/.deploy_key.pub
deploy/server-setup.sh

# nginx 管理介面門禁密鑰（server-only，勿進版控）
deploy/nginx/admin-gate.map

# code graph tool cache
.codegraph/
